# apply via
# conda env create -f environment.yml
# conda activate dagster-asset-demo
# conda remove --name dagster-asset-demo --all
#name: dagster-asset-demo
channels:
  - conda-forge
  - defaults
dependencies:
  - jupyterlab
  - jupyter_contrib_nbextensions
  - nb_conda
  - ipywidgets
  - jupyterlab-git
  - yarn
  - nodejs
  - python>=3.8
  - git
  - mamba
  - pandas
  - pyarrow
  - pygit2
  - sqlalchemy
  - sqlite
  - mock
  - requests
  - ratelimit 
  - pandera
  - pip
  - pip:
      - dagster
      - dagster-shell
      - dagster-pandas
      - dagster-pandera
      - dagster-dbt
      - dbt-core
      - dbt-sqlite
      #- dagster-postgres
      - dagit
      # - acryl-datahub
      # - dagster-dbt
      # - dbt
      # - polars
